# Shared assembly routines

`filetest.inc` implements the file checks shared by CPMTEST and RAMTEST.
`serial_test.inc` supplies serial timing, framing, and polling routines for
SERPINS, SERTX, and SERRX. These are assembly includes, not separate programs.
