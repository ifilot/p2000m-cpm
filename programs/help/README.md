# HELP

`HELP` is the short, offline reference installed on A:. It describes the drive
layout, essential file commands, the bundled utilities, and the SD write-flush
rule. It deliberately fits on one screen; use the repository user guide for
the full reference.
