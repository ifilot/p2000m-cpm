# CDEMO

A BDS C example that computes the sum of squares from 1 to 10 (expected result:
385). Demonstrates the bundled compiler, linker, and runtime with a small program.

On B:, run `CC CDEMO`, then `CLINK CDEMO`, then `CDEMO`.
