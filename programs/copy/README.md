# COPY

Copies a file between CP/M drives using BDOS file operations, refusing to
overwrite an existing destination. Intended as a simple file-copy utility
and an example of FCB-based I/O.

Example: `COPY A:HELLO.COM B:HELLO.COM`.
